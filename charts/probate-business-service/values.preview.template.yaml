java:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_NAME}.preview.platform.hmcts.net
  devcpuRequests: 100m
  devcpuLimits: 500m
  devmemoryRequests: 768Mi
  devmemoryLimits: 1.5Gi
  keyVaults:
    probate:
      secrets:
        - name: probate-notify-invite-apikey
          alias: services.notify.apiKeyPrimary
        - name: probate-notify-invite-apikey-secondary
          alias: services.notify.apiKeySecondary
        - name: s2s-probate-backend
          alias: services.auth.provider.totp_secret
